<form method="post" action="{{ path('member_status_delete', {'id': member_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member_status.id) }}">
    <button class="btn btn-link text-danger"><i class="fas fa-trash fa-fw"></i> Delete</button>
</form>
